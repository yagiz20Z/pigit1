# Derleme düzeltmesi

- `src/beyin.rs`: `base_hiz` değişkeni açıkça `f32` yapıldı.
- `src/veri_tipleri.rs`: kullanılmayan `RotaBelirle` importu kaldırıldı.

Düzeltme:

```rust
let base_hiz: f32 = 400.0;
```
