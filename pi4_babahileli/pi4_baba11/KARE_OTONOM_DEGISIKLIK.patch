--- /tmp/kare_baseline/pi4_baba11/src/beyin.rs	2026-07-20 17:02:49.000000000 +0000
+++ /mnt/data/kare_otonom_work/pi4_baba11/src/beyin.rs	2026-07-20 17:09:57.590301501 +0000
@@ -59,6 +59,28 @@
 // BNO/Pico yayın hızı 1 Hz'e düştüğünde 500 ms watchdog otonomiyi sürekli kesiyordu.
 const IMU_VERI_ZAMAN_ASIMI: Duration = Duration::from_secs(2);
 
+// -----------------------------------------------------------------------------
+// ZAMAN TABANLI KARE TESTİ
+// -----------------------------------------------------------------------------
+// Bu mod GPS, rota ve IMU olmadan çalışır. YKİ'de OTONOM veya START komutu
+// geldiğinde 4 kenarlı açık çevrim kare hareketi başlatılır.
+//
+// İlk su testinde düşük güç kullanın. Teknenin gerçek hızı ve dönüş oranına göre
+// sadece aşağıdaki dört değeri değiştirmeniz yeterlidir.
+const KARE_KENAR_SURESI: Duration = Duration::from_secs(10);
+const KARE_DONUS_SURESI: Duration = Duration::from_millis(2500);
+const KARE_ILERI_PWM: u16 = 400;
+const KARE_SOL_DONUS_PWM: u16 = 400;
+const KARE_KENAR_SAYISI: u8 = 4;
+
+#[derive(Debug, Clone, Copy, PartialEq, Eq)]
+enum KareAsama {
+    Hazir,
+    Ileri,
+    SolaDon,
+    Tamamlandi,
+}
+
 // Varsayılan fiziksel motor sırası:
 // M1 = sola yatay, M2 = sağa yatay, M3 = ileri-1, M4 = ileri-2.
 // Bu sıra YKİ'den CMD:MAP:sol,ileri1,sag,ileri2 komutuyla değiştirilebilir.
@@ -309,6 +331,14 @@
     let mut gorev_baslatma_istendi = false;
     // Otonomdayken oyun kolundan kalan CMD:MAN:0,0 paketlerini yalnız bir kez logla.
     let mut otonomda_manuel_reddedildi = false;
+
+    // GPS/rota bağımsız kare görev durumu. Kenar numarası 1..=4 aralığındadır.
+    let mut kare_aktif = false;
+    let mut kare_tamamlandi = false;
+    let mut kare_kenar_no: u8 = 0;
+    let mut kare_asama = KareAsama::Hazir;
+    let mut kare_asama_baslangic = Instant::now();
+
     println!(
         "Motor eşlemesi başlangıç: sol=M{}, ileri=M{}+M{}, sağ=M{}",
         motor_esleme.sol, motor_esleme.ileri1, motor_esleme.ileri2, motor_esleme.sag,
@@ -348,6 +378,10 @@
                 GelenTelemetri::AcilDurdur => {
                     guncel_mod = AracMod::AcilDurum;
                     gorev_baslatma_istendi = false;
+                    kare_aktif = false;
+                    kare_tamamlandi = false;
+                    kare_asama = KareAsama::Hazir;
+                    kare_kenar_no = 0;
                     son_manuel_ileri = 0.0;
                     son_manuel_yatay = 0.0;
                     manuel_komut_alindi = false;
@@ -355,52 +389,72 @@
                     println!("Acil durdur!");
                 }
                 GelenTelemetri::GoreviBaslat => {
-                    if guncel_mod != AracMod::AcilDurum {
+                    if guncel_mod == AracMod::AcilDurum {
+                        eprintln!("START reddedildi: sistem AcilDurum modunda.");
+                    } else if kare_aktif {
+                        // RF ACK tekrarları aktif görevi sıfırdan başlatmasın.
+                        println!(
+                            "START tekrar alındı; kare görevi {}. kenarda {:?} aşamasında devam ediyor.",
+                            kare_kenar_no, kare_asama,
+                        );
+                    } else {
                         gorev_baslatma_istendi = true;
+                        guncel_mod = AracMod::Otonom;
+                        kare_aktif = true;
+                        kare_tamamlandi = false;
+                        kare_kenar_no = 1;
+                        kare_asama = KareAsama::Ileri;
+                        kare_asama_baslangic = Instant::now();
                         son_manuel_ileri = 0.0;
                         son_manuel_yatay = 0.0;
                         manuel_komut_alindi = false;
                         otonomda_manuel_reddedildi = false;
-
-                        if !nav.hedef_noktalar.is_empty() {
-                            guncel_mod = AracMod::Otonom;
-                            pid.integral = 0.0;
-                            kaba_donus_modu = false;
-                            println!(
-                                "START kabul edildi: {} waypoint ile OTONOM aktif. GPS={} IMU={} Origin={}",
-                                nav.hedef_noktalar.len(),
-                                gps_hazir,
-                                imu_hazir,
-                                nav.is_origin_set,
-                            );
-                        } else {
-                            // START paketi ROTA'dan önce geldiyse komutu kaybetme.
-                            guncel_mod = AracMod::GorevBekliyor;
-                            println!(
-                                "START alındı fakat rota henüz yok; rota gelince otonom otomatik başlayacak."
-                            );
-                        }
-                    } else {
-                        eprintln!("START reddedildi: sistem AcilDurum modunda.");
+                        pid.integral = 0.0;
+                        kaba_donus_modu = false;
+                        println!(
+                            "KARE OTONOM BAŞLADI: 4 x {} sn ileri, dönüşler {} ms, ileri PWM={}, sol dönüş PWM={}. GPS/IMU/ROTA kullanılmıyor.",
+                            KARE_KENAR_SURESI.as_secs(),
+                            KARE_DONUS_SURESI.as_millis(),
+                            KARE_ILERI_PWM,
+                            KARE_SOL_DONUS_PWM,
+                        );
                     }
                 }
                 GelenTelemetri::ModDegistir(istenen_mod) => {
-                    guncel_mod = if istenen_mod == AracMod::Otonom {
-                        gorev_baslatma_istendi = true;
-                        if nav.hedef_noktalar.is_empty() {
+                    if istenen_mod == AracMod::Otonom {
+                        if guncel_mod == AracMod::AcilDurum {
+                            eprintln!(
+                                "OTONOM reddedildi: önce acil durumu kaldırıp Manuel moda geçin."
+                            );
+                        } else if kare_aktif {
                             println!(
-                                "OTONOM isteği alındı fakat rota yok; rota gelince otomatik başlayacak."
+                                "OTONOM tekrar alındı; kare görevi {}. kenarda {:?} aşamasında devam ediyor.",
+                                kare_kenar_no, kare_asama,
                             );
-                            AracMod::GorevBekliyor
                         } else {
+                            gorev_baslatma_istendi = true;
+                            guncel_mod = AracMod::Otonom;
+                            kare_aktif = true;
+                            kare_tamamlandi = false;
+                            kare_kenar_no = 1;
+                            kare_asama = KareAsama::Ileri;
+                            kare_asama_baslangic = Instant::now();
                             pid.integral = 0.0;
                             kaba_donus_modu = false;
-                            AracMod::Otonom
+                            println!(
+                                "OTONOM TUŞU: GPS/IMU/ROTA bağımsız kare görevi başladı. Kenar 1/4 ileri."
+                            );
                         }
                     } else {
+                        // Manuel, görev bekleme veya acil durum seçildiğinde zamanlı görevi
+                        // anında iptal et; motor_istek bu turda sıfıra düşer.
+                        kare_aktif = false;
+                        kare_tamamlandi = false;
+                        kare_kenar_no = 0;
+                        kare_asama = KareAsama::Hazir;
                         gorev_baslatma_istendi = false;
-                        istenen_mod
-                    };
+                        guncel_mod = istenen_mod;
+                    }
                     son_manuel_ileri = 0.0;
                     son_manuel_yatay = 0.0;
                     manuel_komut_alindi = false;
@@ -464,6 +518,10 @@
                 }
                 GelenTelemetri::TelemetriKoptu => {
                     telemetri_bagli = false;
+                    kare_aktif = false;
+                    kare_tamamlandi = false;
+                    kare_asama = KareAsama::Hazir;
+                    kare_kenar_no = 0;
                     son_manuel_ileri = 0.0;
                     son_manuel_yatay = 0.0;
                     manuel_komut_alindi = false;
@@ -507,38 +565,19 @@
                 }
                 GelenTelemetri::RotaBelirle(noktalar) => {
                     let ayni_rota = rotalar_ayni(&nav.hedef_noktalar, &noktalar);
-
-                    if ayni_rota && matches!(guncel_mod, AracMod::Otonom | AracMod::EveDonus) {
-                        // ACK kaybı/retry nedeniyle gelen aynı rota aktif görevi sıfırlamasın.
+                    if !ayni_rota {
+                        nav.set_rota(noktalar);
                         println!(
-                            "Aynı rota tekrar alındı; aktif görev ve waypoint indeksi korunuyor."
+                            "Rota kaydedildi: {} nokta. KARE TEST MODU rotayı kullanmaz.",
+                            nav.hedef_noktalar.len(),
                         );
                     } else {
-                        nav.set_rota(noktalar);
-                        son_manuel_ileri = 0.0;
-                        son_manuel_yatay = 0.0;
-                        manuel_komut_alindi = false;
-                        pid.integral = 0.0;
-                        kaba_donus_modu = false;
+                        println!("Aynı rota tekrar alındı; mevcut kayıt korunuyor.");
+                    }
 
-                        if guncel_mod != AracMod::AcilDurum && gorev_baslatma_istendi {
-                            guncel_mod = AracMod::Otonom;
-                            println!(
-                                "Yeni rota alındı: {} nokta. Bekleyen START/MOD isteğiyle OTONOM başladı.",
-                                nav.hedef_noktalar.len(),
-                            );
-                        } else if guncel_mod != AracMod::AcilDurum {
-                            guncel_mod = AracMod::GorevBekliyor;
-                            println!(
-                                "Yeni rota alındı: {} nokta. START bekleniyor.",
-                                nav.hedef_noktalar.len(),
-                            );
-                        } else {
-                            println!(
-                                "Yeni rota alındı: {} nokta; AcilDurum nedeniyle motorlar kapalı.",
-                                nav.hedef_noktalar.len(),
-                            );
-                        }
+                    if !kare_aktif && guncel_mod != AracMod::AcilDurum {
+                        guncel_mod = AracMod::GorevBekliyor;
+                        println!("OTONOM veya START komutu bekleniyor.");
                     }
                 }
             }
@@ -551,6 +590,70 @@
             // mod değişimi gelene kadar aynı PWM her 50 ms motor katmanına gönderilir.
             motor_istek = manuel_motor_karistir(son_manuel_ileri, son_manuel_yatay, motor_esleme);
         }
+
+        // ---------------------------------------------------------------------
+        // GPS/IMU/ROTA BAĞIMSIZ KARE HAREKETİ
+        // ---------------------------------------------------------------------
+        // Motor komutu her 50 ms yeniden üretilir. Acil durdurma, Manuel moda
+        // geçiş veya telemetri kopması kare_aktif'i false yaparak aynı turda keser.
+        if guncel_mod == AracMod::Otonom && kare_aktif {
+            match kare_asama {
+                KareAsama::Ileri => {
+                    if kare_asama_baslangic.elapsed() >= KARE_KENAR_SURESI {
+                        if kare_kenar_no >= KARE_KENAR_SAYISI {
+                            kare_aktif = false;
+                            kare_tamamlandi = true;
+                            kare_asama = KareAsama::Tamamlandi;
+                            guncel_mod = AracMod::GorevBekliyor;
+                            gorev_baslatma_istendi = false;
+                            println!(
+                                "KARE GÖREVİ TAMAMLANDI: 4. kenar bitti, bütün motorlar durduruldu."
+                            );
+                        } else {
+                            kare_asama = KareAsama::SolaDon;
+                            kare_asama_baslangic = Instant::now();
+                            println!(
+                                "KARE: {}. kenar tamamlandı; {} ms sola dönüş başladı.",
+                                kare_kenar_no,
+                                KARE_DONUS_SURESI.as_millis(),
+                            );
+                        }
+                    } else {
+                        motor_kanalina_yaz(
+                            &mut motor_istek,
+                            motor_esleme.ileri1,
+                            KARE_ILERI_PWM,
+                        );
+                        motor_kanalina_yaz(
+                            &mut motor_istek,
+                            motor_esleme.ileri2,
+                            KARE_ILERI_PWM,
+                        );
+                    }
+                }
+                KareAsama::SolaDon => {
+                    if kare_asama_baslangic.elapsed() >= KARE_DONUS_SURESI {
+                        kare_kenar_no += 1;
+                        kare_asama = KareAsama::Ileri;
+                        kare_asama_baslangic = Instant::now();
+                        println!(
+                            "KARE: sola dönüş tamamlandı; {}. kenar ileri başladı.",
+                            kare_kenar_no,
+                        );
+                    } else {
+                        // Fiziksel yerleşimde sola döndüren kanal ters ise aşağıdaki
+                        // motor_esleme.sol ifadesini motor_esleme.sag olarak değiştirin.
+                        motor_kanalina_yaz(
+                            &mut motor_istek,
+                            motor_esleme.sol,
+                            KARE_SOL_DONUS_PWM,
+                        );
+                    }
+                }
+                KareAsama::Hazir | KareAsama::Tamamlandi => {}
+            }
+        }
+
         if !nav.is_origin_set {
             if gps_yeni
                 && gps_hazir
@@ -589,7 +692,7 @@
                 nav.guncelle_konum(gps.enlem, gps.boylam, imu.yaw as f64);
                 anlik_hedef_aci = imu.yaw as f64;
 
-                if matches!(guncel_mod, AracMod::Otonom | AracMod::EveDonus) {
+                if guncel_mod == AracMod::EveDonus || (guncel_mod == AracMod::Otonom && !kare_aktif) {
                     let hedef = if guncel_mod == AracMod::EveDonus {
                         eve_donus_noktasi
                     } else {
@@ -691,8 +794,11 @@
                         }
                     }
                 }
-            } else if matches!(guncel_mod, AracMod::Otonom | AracMod::EveDonus) {
-                // motor_istek varsayılan sıfır olarak kalır. Sensör geri gelince mod
+            } else if guncel_mod == AracMod::EveDonus
+                || (guncel_mod == AracMod::Otonom && !kare_aktif)
+            {
+                // Motor isteği varsayılan sıfır olarak kalır. Zamanlı kare görevi
+                // sensör watchdog'una bağlı değildir; GPS/waypoint modu bağlıdır.
                 // korunarak hesap otomatik devam eder.
                 if !sensor_guvenlik_bildirildi {
                     eprintln!(
@@ -735,21 +841,23 @@
             gps_hazir.then_some(&gps),
         );
 
-        // Otonom seçili olduğu halde PWM sıfırsa kullanıcı yalnız "bağlandı" bilgisini
-        // görüp kök nedeni kaçırmasın. Her 2 saniyede bir tüm izin koşullarını yazdır.
-        if guncel_mod == AracMod::Otonom && telemetri_sayaci % 40 == 0 {
+        // Kare görevin hangi kenar/aşamada olduğunu ve gerçek motor isteğini yazdır.
+        if guncel_mod == AracMod::Otonom && kare_aktif && telemetri_sayaci % 20 == 0 {
+            let kalan_ms = match kare_asama {
+                KareAsama::Ileri => KARE_KENAR_SURESI
+                    .saturating_sub(kare_asama_baslangic.elapsed())
+                    .as_millis(),
+                KareAsama::SolaDon => KARE_DONUS_SURESI
+                    .saturating_sub(kare_asama_baslangic.elapsed())
+                    .as_millis(),
+                _ => 0,
+            };
             println!(
-                "OTONOM DURUM | rota={}/{} GPS[taze={},fix={},uydu={},hazır={}] IMU[taze={},hazır={}] origin={} hedef_mesafe={:.2}m PWM=[{},{},{},{}]",
-                nav.current_hn_index,
-                nav.hedef_noktalar.len(),
-                gps_taze,
-                gps.algi_boyut,
-                gps.uydu_sayi,
-                gps_hazir,
-                imu_taze,
-                imu_hazir,
-                nav.is_origin_set,
-                anlik_hedef_mesafe,
+                "KARE DURUM | kenar={}/{} aşama={:?} kalan={}ms PWM=[{},{},{},{}]",
+                kare_kenar_no,
+                KARE_KENAR_SAYISI,
+                kare_asama,
+                kalan_ms,
                 motor_istek.iskeleon,
                 motor_istek.iskelearka,
                 motor_istek.sancakon,
@@ -761,35 +869,29 @@
 
         telemetri_sayaci += 1;
         if telemetri_sayaci % 2 == 0 {
-            let toplam_waypoint = nav.hedef_noktalar.len();
-            let aktif_waypoint = if toplam_waypoint == 0 {
-                0
-            } else if nav.current_hn_index < toplam_waypoint {
-                nav.current_hn_index + 1
+            let (toplam_waypoint, aktif_waypoint) = if kare_aktif || kare_tamamlandi {
+                (KARE_KENAR_SAYISI as usize, kare_kenar_no.max(1) as usize)
             } else {
-                toplam_waypoint
+                let toplam = nav.hedef_noktalar.len();
+                let aktif = if toplam == 0 {
+                    0
+                } else if nav.current_hn_index < toplam {
+                    nav.current_hn_index + 1
+                } else {
+                    toplam
+                };
+                (toplam, aktif)
             };
 
             let otonom_durum = if guncel_mod == AracMod::AcilDurum {
                 8
-            } else if guncel_mod == AracMod::Otonom {
-                if toplam_waypoint == 0 {
-                    1
-                } else if !gps_hazir {
-                    2
-                } else if !imu_hazir {
-                    3
-                } else if !nav.is_origin_set {
-                    4
-                } else if kaba_donus_modu {
-                    5
-                } else {
-                    6
+            } else if guncel_mod == AracMod::Otonom && kare_aktif {
+                match kare_asama {
+                    KareAsama::SolaDon => 5,
+                    KareAsama::Ileri => 6,
+                    _ => 0,
                 }
-            } else if guncel_mod == AracMod::GorevBekliyor
-                && toplam_waypoint > 0
-                && nav.current_hn_index >= toplam_waypoint
-            {
+            } else if kare_tamamlandi {
                 7
             } else {
                 0
@@ -807,7 +909,11 @@
                     _ => 0.0,
                 },
                 imu_veri: (imu.roll, imu.pitch, imu.yaw),
-                setpoint_yaw: anlik_hedef_aci as f32,
+                setpoint_yaw: if kare_aktif {
+                    imu.yaw
+                } else {
+                    anlik_hedef_aci as f32
+                },
                 arac_mod: guncel_mod,
                 aktif_waypoint,
                 toplam_waypoint,
